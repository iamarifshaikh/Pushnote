export class Problem {
  constructor(status, message) {
    this.status = status;
    this.message = message;
  }
}

export class Success {
  constructor(status, message) {
    this.status = status;
    this.message = message;
  }
}
